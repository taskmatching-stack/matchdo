# 攝影模擬機身圖（目前實際路徑）

檔案放在 **`public/img/`**（根目錄 img，非本子資料夾）：

| 檔名 | 用途 |
|------|------|
| `cam-film.png` | 底片模擬 |
| `cam-lcd-off.png` | 數位／品牌色彩，螢幕關 |
| `cam-lcd-on.png` | 數位／品牌色彩，螢幕亮（參數文字疊在螢幕區） |

LCD 位置微調：`public/css/promo-camera.css` → `.pc-camera-lcd-wrap` 的 `left` / `top` / `width` / `height`。
