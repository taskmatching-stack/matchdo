---
description: FLUX／Gemini 提示詞 — 嚴禁查表式硬編碼（檔名 regex、material_key 表面句）
alwaysApply: true
---

# FLUX／Gemini 提示詞（必守）

改 **材料 optimize**、**設計頁生圖**（`composeGeneratePromptWithReferences`）、`buildMaterial*`、`visual-semantics.js` 前，**先讀** `docs/flux-and-gemini-prompt-policy.md`。

## 嚴禁（查表式硬編碼 = 垃圾）

- ❌ 檔名／標題 **regex** → 推斷皮革／布料／木材再送 FLUX
- ❌ `material_key` **枚舉 map** → 固定英文表面／尺度句
- ❌ 程式 **紋理族分類** 刪改 Gemini 的 `patterns`
- ❌ 無 `image_semantics_json` 仍靜默跑材料 FLUX
- ❌ 擅自改 FLUX **1024×1024** 或未文件化輸出尺寸

## 唯一正確（材料表面 optimize）

**原圖 → `buildVendorAssetMaterialFluxOptimizePrompt`（中文，`material_surface_type`）→ FLUX 1024²**

標籤管線：**原圖 → Gemini（`material_tagging_prompt`）→ `image_semantics_json` → `buildMaterialFluxFidelityLine`**（僅設計頁生圖附錄）

標題／檔名／分類只准作 **Gemini 補充**（以影像為準）。矛盾改 **Gemini prompt 自洽規則**，不加後端第二套表。

## 允許

- DB 分類 `prompt`、使用者描述、工藝 `visual_hint`（DB）
- 篩選用 `material_key`／`color_key`（**不進** FLUX prompt）
- 短通用 img2img 底稿、參考圖 **角色** 句（非具體皮種）
- **原圖印刷**：通用 **surface graphic / artwork** 句（見 `docs/custom-product-reference-pattern-prompt-policy.md`）

## 嚴禁（原圖印刷額外）

- ❌ 把原圖印刷窄化成 **Logo lockup / wordmark** 專用 prompt block
- ❌ 前端自動灌 **image 1=…→** 中文對照進使用者 prompt

## 嚴禁（訂製品 2×2 Split-view — 200 子分類）

改 **設計頁生圖** `buildFluxCatalogCompositeRefLead`、`composeGeneratePromptWithReferences` 前，**先讀** `docs/custom-product-subcategory-prompt-guide.md` **§7**。

- ❌ 在 `server.js` **寫死或強化** Split-view 1～4、四格視角、鏡位互斥（如「左下與右下不得相同」）— **只准改後台子分類 DB `prompt`**
- ❌ 為修**單一分類**案例在後端加全域四格構圖句 — 會與其他子分類矛盾
- ✅ 後端僅維護**通用**參考圖附錄（特徵擷取、四格同一成品、不重繪參考圖構圖）
