---
description: 部署方式：Google Cloud Shell + Cloud Run，絕對不要說本機 gcloud 或 Cloud Build 自動觸發
alwaysApply: true
---

# 部署方式（只有這一種，永遠不要說錯）

## 部署前必做（Agent 責任，不可跳過）

Cloud Shell 的 `git fetch origin main && reset --hard` **只會拉到遠端已有之 commit**。  
因此 **禁止**在尚未 push 時叫使用者部署。

正確順序（每次改完要上線時）：

1. **本機**：完成程式修改 → `git add` → `git commit`（僅含要上線的檔案）
2. **`git push origin main`**，並確認成功（`git status` 顯示與 `origin/main` 同步）
3. **才**提供下方 Cloud Shell 一行指令，並註明「已 push 的 commit」或 hash

若 push 失敗、未 commit、或使用者明確說不要 push：**只說明狀態與待辦，不要給部署指令**。

## 正確指令：Google Cloud Shell

```bash
cd ~/matchdo && git fetch origin main && git reset --hard origin/main && gcloud run deploy matchdo --source . --region=asia-northeast1 --allow-unauthenticated --clear-base-image
```

在瀏覽器開 **Google Cloud Shell** 貼上執行，一行搞定。  
（用 `fetch + reset --hard` 避免本地與 origin 分叉時 pull 失敗。）

## 絕對禁止

- ❌ **未 push 就請使用者去 Cloud Shell 部署**（等於部署舊版，浪費時間）
- ❌ 寫「請先 push 再部署」卻自己不 push（除非使用者禁止 push 或權限不足）
- ❌ 不要說「本機 PowerShell 執行 gcloud」（使用者電腦沒有安裝 gcloud）
- ❌ 不要說「Cloud Build 自動觸發，push 就好」（觸發條件未設定）
- ❌ 不要說任何其他部署方式

## 參考文件

`docs/deploy-zeabur-github.md` — GCP Cloud Run 方案二
