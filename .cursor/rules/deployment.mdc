---
description: 部署方式：先 push GitHub，再 Cloud Shell 或本機 gcloud（從 origin/main 部署）
alwaysApply: true
---

# 部署方式

完整說明見 **`docs/deploy-matchdo-push-and-deploy.md`**（繁體中文）。

## ⛔ 使用者一再提醒（Agent 必守，違反 = 嚴重失誤）

**Cloud Shell 部署指令：只能給下方「靜音版整行」，禁止給任何不含 `grep` 的簡化版。**

使用者已知 `Regional Access Boundary` / `taskmatchlng` 是假錯誤；他們要的是**靜音輸出**，不是再解釋一次「沒關係」。**不要再忽略此要求。**

**給使用者時，原樣貼下面兩段（先 account，再整行 deploy）：**

```bash
gcloud config set account taskmatching@gmail.com
gcloud config set project matchdo
```

```bash
cd ~/matchdo && git fetch origin main && git reset --hard origin/main && ( gcloud run deploy matchdo --source . --region=asia-northeast1 --allow-unauthenticated --clear-base-image && gcloud run services update-traffic matchdo --region=asia-northeast1 --to-latest ) 2>&1 | grep --line-buffered -v -E 'Regional Access Boundary|taskmatchlng'
```

**禁止**在回覆中改寫、縮短、或刪掉末尾 `grep`（含「為了簡潔」）。

## 部署前必做（Agent 責任）

1. 本機 commit → **`git push origin main`** 成功
2. 註明 commit hash（`git log -1 --oneline`）
3. **才**提供部署指令

禁止未 push 就叫使用者部署。

## Cloud Shell 部署（Agent 給使用者時必用 §3.1 整行）

**禁止**提供下列任一種（屢犯、會滿屏紅字或誤判失敗）：

- ❌ 不含 `grep` 的 deploy 單行
- ❌ `grep -v -E 'Downloading|Extracting|Verifying|Already exists|Pull complete|Status:'` — **過濾不了** `Regional Access Boundary`
- ❌ `gcloud run deploy … 2>&1 | grep … && gcloud run services update-traffic …` — grep 須包 **整段** `( deploy && update-traffic )`

**唯一合法 grep 尾綴：**

```text
) 2>&1 | grep --line-buffered -v -E 'Regional Access Boundary|taskmatchlng'
```

**Cloud Shell 整行（從 `docs/deploy-matchdo-push-and-deploy.md` §3.1 複製，勿改）：**

```bash
gcloud config set account taskmatching@gmail.com
gcloud config set project matchdo
cd ~/matchdo && git fetch origin main && git reset --hard origin/main && ( gcloud run deploy matchdo --source . --region=asia-northeast1 --allow-unauthenticated --clear-base-image && gcloud run services update-traffic matchdo --region=asia-northeast1 --to-latest ) 2>&1 | grep --line-buffered -v -E 'Regional Access Boundary|taskmatchlng'
```

（成功以 **`Done.`** 為準；`grep` 只藏已知噪音。）

**§3.1 單行 `--source` 失敗時** — 用 deploy 文件 **§3.3** 兩段式整行（**同樣含上述 `grep`**，勿貼無過濾版）。

**本機 PowerShell**：見 deploy 文件 **§3.2**（通常不需 `grep`）；參數須與 §3.1 一致（含 `--clear-base-image`）。

## 部署後自查

```bash
gcloud run services describe matchdo --region=asia-northeast1 --format='yaml(spec.traffic,status.latestReadyRevisionName)'
```

## 禁止

- ❌ Cloud Shell 給**不含** `Regional Access Boundary|taskmatchlng` 過濾的 deploy 指令
- ❌ 用 Docker 關鍵字 `grep` 代替上述過濾
- ❌ 在 PROGRESS／handoff 文件**另寫一套** deploy 單行 — 只連結 **§3.1**
- ❌ 平常用 `update-traffic` **代替**部署（僅 §5.2 排除流量釘死時可用）
- ❌ 未 push 就部署
- ❌ 跳過 `git fetch` + `reset --hard` 直接 deploy 本機未同步目錄
