---
description: 部署方式：先 push GitHub，再 Cloud Shell 或本機 gcloud（從 origin/main 部署）
alwaysApply: true
---

# 部署方式

完整說明見 **`docs/deploy-matchdo-push-and-deploy.md`**（繁體中文）。

## 部署前必做（Agent 責任）

1. 本機 commit → **`git push origin main`** 成功
2. 註明 commit hash（`git log -1 --oneline`）
3. **才**提供部署指令

禁止未 push 就叫使用者部署。

## 正確部署指令（Cloud Shell 或本機 PowerShell 相同邏輯）

**Cloud Shell：**

```bash
cd ~/matchdo && git fetch origin main && git reset --hard origin/main && gcloud run deploy matchdo --source . --region=asia-northeast1 --allow-unauthenticated --clear-base-image && gcloud run services update-traffic matchdo --region=asia-northeast1 --to-latest
```

（末尾 `update-traffic --to-latest`：避免流量釘在舊 revision、deploy 顯示 Done 卻沒換程式。）

**單行失敗時（上傳卡住、`Container import failed`、Shell 中斷）**：改 **§三 方式 C 兩段式**（`gcloud builds submit --config=cloudbuild.yaml` → `deploy --image` → `update-traffic`）。詳見 `docs/deploy-matchdo-push-and-deploy.md`。

**本機 PowerShell（Shell 額度滿時）：** 與 Cloud Shell **同一套 `gcloud run deploy` 參數**（含 `--clear-base-image`）。先 `gcloud config set account taskmatching@gmail.com`、`gcloud config set project matchdo`，再於專案目錄 `git fetch` + `reset --hard origin/main` + 部署（見 `docs/deploy-matchdo-push-and-deploy.md` 方式 B）。

## 部署後自查

```bash
gcloud run services describe matchdo --region=asia-northeast1 --format='yaml(spec.traffic,status.latestReadyRevisionName)'
```

`spec.traffic` 應指向**本次新 revision**；若永遠釘在同一個舊 `revisionName`，見 `docs/deploy-matchdo-push-and-deploy.md` **§五**（tag + `update-traffic` 排除流量釘死）。

## 禁止

- ❌ 平常用 `update-traffic` **代替**部署（僅 §五 排除流量釘死時可用）
- ❌ 未 push 就部署
- ❌ 未設觸發卻說「push 就好」
- ❌ 跳過 `git fetch` + `reset --hard` 直接 deploy 本機未同步目錄
- ❌ **現在**建立 Cloud Build GitHub 自動觸發（見 `docs/deploy-matchdo-push-and-deploy.md` §六：日後再做）
