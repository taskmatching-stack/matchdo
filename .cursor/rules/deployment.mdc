---
description: 部署方式：先 push GitHub，再 Cloud Shell 或本機 gcloud（從 origin/main 部署）
alwaysApply: true
---

# 部署方式

完整說明見 **`docs/deploy-matchdo-push-and-deploy.md`**（繁體中文）。

## 部署前必做（Agent 責任）

1. 本機 commit → **`git push origin main`** 成功
2. 註明 commit hash（`git log -1 --oneline`）
3. **才**提供部署指令

禁止未 push 就叫使用者部署。

## 正確部署指令（Cloud Shell 或本機 PowerShell 相同邏輯）

**Cloud Shell：**

```bash
cd ~/matchdo && git fetch origin main && git reset --hard origin/main && gcloud run deploy matchdo --source . --region=asia-northeast1 --allow-unauthenticated
```

**本機 PowerShell（Shell 額度滿時）：** 先 `gcloud config set account taskmatching@gmail.com`、`gcloud config set project matchdo`，再於專案目錄執行 `git fetch` + `reset --hard origin/main` + 同上 `gcloud run deploy`（見文件）。

## 禁止

- ❌ 加 `--clear-base-image`
- ❌ 未 push 就部署
- ❌ 未設觸發卻說「push 就好」
- ❌ 跳過 `git fetch` + `reset --hard` 直接 deploy 本機未同步目錄
