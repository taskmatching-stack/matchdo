---
description: 攝影模擬線上三入口凍結；Store 前置只在 L4（apps/matchdo-promo-camera）開發
alwaysApply: true
---

# 攝影模擬 App 隔離（必守）

## 四個表面

| 表面 | URL / 位置 | Store 前置期間 |
|------|------------|----------------|
| **原站** | `/promo-camera` | **凍結** |
| **設計 embed** | `/promo-camera?embed=design` | **凍結** |
| **線上 PWA** | `/promo-camera-app` | **凍結** |
| **Store 實驗** | `apps/matchdo-promo-camera/` | **只准改這裡** + L4 專用 JS |

**隔離層文件（必先讀）：** `docs/PLAN-promo-camera-app-isolation-layer.md`  
**進度 handoff：** `docs/PROGRESS-promo-camera-app-store.md`（接續開發從此看）  
**基準 commit：** `f5dceeb`（建 L4 前）；**L4 完成後遠端 HEAD：** `922b8ef`

## 線上 PWA 檔（L3b — 凍結，Store 前置期禁止改）

- `public/client/promo-camera-app.html`
- `public/css/promo-camera-app.css`
- `public/js/promo-camera/app-shell.js`
- `public/js/promo-camera/pwa-install-prompt.js`（僅獨立頁；`embed=design` 不載入）
- `public/promo-camera-app.webmanifest`

## Store 前置檔（L4 — 可擴充，未掛線上 PWA）

- `apps/matchdo-promo-camera/`
- `public/js/promo-camera/app-runtime.js`（**禁止**加入 `promo-camera-app.html` script 列表）
- `public/js/promo-camera/app-native-bridge.js`（同上，待實作原生 API）

## 共用檔（Store 需求禁止改，除非修三入口都有的 bug）

- `public/client/promo-camera.html`
- `public/css/promo-camera.css`
- `public/js/promo-camera/index.js`
- `public/js/promo-camera/state.js`
- `public/js/promo-camera/api.js`
- `public/js/promo-camera/presets.js`
- `public/js/custom-product.js`

## 禁止再犯

| 擅自改動 | 後果 |
|----------|------|
| 為 Store 改 `promo-camera-app.html` / `app-shell.js` | 線上 PWA 被改壞 |
| 把 `app-runtime.js` 掛進線上 PWA | 未驗證行為上線 |
| 在 `promo-camera.css` / `index.js` 加 Store 分支 | 原站／embed／PWA 連帶回歸 |
| 未核對 git 與雲端一致就改 L3 | 基準漂移 |

## 正確做法

1. **Store 實驗** → `apps/matchdo-promo-camera/` 內 Capacitor + 自 L3b 複製快照
2. **原生橋** → L4 的 `app-runtime.js` / `app-native-bridge.js`，僅 Store bundle 載入
3. **線上 bug** → 修共用檔時必查 `/promo-camera` 與 `/promo-camera-app` 與改前一致
4. 建 L4 前：`git fetch origin main` 且 `HEAD` = `origin/main`，working tree clean

## 參考

- **進度 handoff：** `docs/PROGRESS-promo-camera-app-store.md`
- 隔離層：`docs/PLAN-promo-camera-app-isolation-layer.md`
- Capacitor：`docs/PLAN-promo-camera-capacitor-app.md`
- 歷史還原：`e3c5e6c`
