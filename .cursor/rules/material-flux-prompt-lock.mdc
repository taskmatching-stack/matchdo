# 材料 AI 重繪 prompt — 已確定（2026-08-08 使用者定案）

## 必守

使用者已定案材料色卡重繪語意。**禁止** Agent 擅自改回舊版中文兩句或查表式硬編碼。

| 項目 | 鎖定值 |
|------|--------|
| 提示詞 | `buildVendorAssetMaterialFluxOptimizePrompt` — **英文 swatch prompt**；`material_surface_type` **建議填寫**（較精準；不強制，可留空由 AI 從圖判斷） |
| 原色 | **維持圖中該材質的原色與質感**（含金屬反光、透明）；非全局改色 |
| 禁止 | 文字、Logo、印花圖案 |
| 引擎 | Gemini 優先 → FLUX 備援（`vendor_asset_optimize_engine`） |
| FLUX SEED | `3647440197`（`VENDOR_MATERIAL_FLUX_SEED`） |
| 送 BFL | 英文原句，`skipPromptTranslation: true` |
| 輸出尺寸 | 1024×1024 |

函式：`buildVendorAssetMaterialFluxOptimizePrompt`、`optimizeVendorAssetImage`（材料分支）、`optimizeVendorAssetImageWithFlux`（材料分支）。

## 允許改動的前提

- 使用者**當次對話明確**要求改提示詞、SEED、尺寸或管線
- 改動須在回覆中**列出改前／改後**，並更新 UI 提示詞預覽文案

## 禁止

- ❌ 改回「保持顏色並優化此{材質}材質光影」舊句（除非使用者再要求）
- ❌ 用 `material_key`／檔名查表推斷材質送 prompt
- ❌ 禁止 3D／陰影（金屬反光、透明需保留）
- ❌ 把材料組合、原型重繪、標籤管線一併改掉
