# 穩定基準版本（實作前快照）

> **用途**：後續實作「操作流程更清晰」與「廠商素材庫」時，以本版本為穩定基準；若改動導致問題，可依此還原或比對。
> **原則**：**不破壞目前穩定性**；改動以**加法為主**（新增提示、選單語意、步驟條），避免重構既有關鍵流程。

---

## 一、基準版本

| 項目 | 值 |
|------|-----|
| **Commit** | `b27ce73` |
| **完整 SHA** | `b27ce730b2a7db9977f6477e25ad6735b51c6e89` |
| **說明** | feat: 圖樣提取改為僅能從數位資產選擇圖片（避免侵權），實境模擬維持可上傳 |
| **紀錄日期** | 2026-03-13 |

還原指令（若需回到此版本）：
```bash
git checkout b27ce73 -- <path>
# 或整庫回退（慎用）：
# git reset --hard b27ce73
```

---

## 二、安全實作原則

| 原則 | 說明 |
|------|------|
| **不更動首頁靈感牆** | 首頁（iStudio/index、媒體牆、篩選、卡片、lightbox、URL）版面與邏輯維持不變。 |
| **加法優先** | 新增：步驟條、提示文案、選單分組／語意；盡量不刪改既有 API 或核心流程。 |
| **小步提交** | 每完成一個可獨立驗證的小功能就 commit，方便 bisect 或回退。 |
| **不重構關鍵路徑** | 產品設計生成／分析、廠商作品上傳、登入／點數等核心流程不重寫，僅在周邊加提示或入口。 |
| **可關閉或可選** | 若新 UI（如步驟條）可能影響版面，優先做成可關閉或僅在特定情境顯示，以保留舊行為。 |

---

## 三、實作順序建議（依規格）

1. **操作流程清晰化**（低風險）  
   - 導覽選單語意／分組微調（不砍既有連結）。  
   - 產品設計頁、廠商端「步驟條」與「建議下一步」提示（新增 DOM／文案，不改既有表單邏輯）。

2. **廠商素材庫**（需新表與 API）  
   - 依 `docs/設計與開店路徑-廠商素材庫規格.md` 階段 1～4 進行；每階段獨立上線與驗證，再進行下一階段。

---

## 四、修訂紀錄

| 日期 | 說明 |
|------|------|
| 2026-03-13 | 建立基準：commit b27ce73，並寫入安全實作原則與實作順序。 |
