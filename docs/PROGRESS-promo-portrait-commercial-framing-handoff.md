# 人像商用構圖防護 — 交接（2026-09-12）

**測試基準：全份回到 `35316b8`（拿掉道具、取消再送出）。**

- 人像 prompt 組裝、描述審核、攝影頁提示文案：皆對齊 `35316b8`
- **例外保留：** `78bec40` 首頁「我的收藏」不被 media wall 覆寫（`public/iStudio-1.0.0/index.html`）
- 線上必須 Cloud Shell 部署後才會換成這版；只 push git 不會改 matchdo.cc
