# MatchDO 上線前檢查表

> **適用**：主站 matchdo.cc 正式／Beta 開放付費用戶  
> **建立**：2026-08-07  
> **基準 commit**：`baa507b`（生圖履歷 P3 + 功能說明拆分）  
> **部署說明**：`docs/deploy-matchdo-push-and-deploy.md` §3.1  
> **功能對照**：`docs/網站功能說明-使用者版.md`、`docs/網站功能說明-管理員版.md`

勾選方式：`- [ ]` 未完成　`- [x]` 已完成

---

## A. 部署前（本機／GitHub）

- [ ] `git status` 乾淨；`main` 已 push 至 `origin/main`
- [ ] 記錄本次上線 commit：`git log -1 --oneline` → `________________`
- [ ] 必要 env 已在 Cloud Run／Secret 設定（Supabase、BFL、Gemini、PayPal 等）
- [ ] `node --check server.js` 通過（可選，本機快速檢查）

---

## B. 部署（Cloud Shell）

```bash
gcloud config set account taskmatching@gmail.com
gcloud config set project matchdo
```

```bash
cd ~/matchdo && git fetch origin main && git reset --hard origin/main && ( gcloud run deploy matchdo --source . --region=asia-northeast1 --allow-unauthenticated --clear-base-image && gcloud run services update-traffic matchdo --region=asia-northeast1 --to-latest ) 2>&1 | grep --line-buffered -v -E 'Regional Access Boundary|taskmatchlng'
```

- [ ] 部署輸出以 **`Done.`** 結束
- [ ] 流量指向最新 revision：

```bash
gcloud run services describe matchdo --region=asia-northeast1 --format='yaml(spec.traffic,status.latestReadyRevisionName)'
```

- [ ] 首頁 `https://matchdo.cc/` 可開（非 5xx）
- [ ] 靜態資源有更新（可對照某頁 `__MATCHDO_*_BUILD` 或硬重新整理）

---

## C. 資料庫 Migration（後台 → 資料庫維護）

路徑：`/admin/db-migrations.html`  
**每項執行後**在後台確認狀態為已套用。

### C1. 本次上線必跑

| id | 用途 | 勾選 |
|----|------|------|
| `provenance-resume-fields` | 生圖履歷：扣點 FK、composed prompt、衍生鏈 | [ ] |

### C2. 若尚未套用（依現網 DB 狀態勾）

| id | 用途 | 勾選 |
|----|------|------|
| `user-material-combo-generations` | 材料組合資產庫表 | [ ] |
| `user-print-generations` | 印花資產庫表 | [ ] |
| `user-asset-library-category` | 資產庫分類欄 | [ ] |
| `user-material-presets` | 材料組合常用文字 | [ ] |
| `material-color-palettes` | 官方配色範例表 | [ ] |
| `material-color-palette-ratios` | 配色比重欄 | [ ] |
| `material-color-palette-i18n` | 配色多語系 | [ ] |
| `material-color-palette-notes` | 配色備註 | [ ] |
| `material-color-palette-examples-seed` | 配色種子資料（可選） | [ ] |

- [ ] 後台 migration 列表無**本次功能依賴**仍顯示「未套用」

---

## D. ① 訂製者 — 煙霧測試

使用 **一般付費測試帳**（非 admin）。

### D1. 帳號與點數

- [ ] 登入 `/login.html`
- [ ] `/credits.html` 餘額顯示正常
- [ ] `/subscription-plans.html` 方案頁可開

### D2. 產品設計

- [ ] `/custom-product.html` 選分類 + 描述 + **生圖成功**
- [ ] 扣點後餘額減少（admin 免扣除外）
- [ ] 「我的數位資產 → 設計稿」出現新圖
- [ ] 卡片 **「履歷」** 可開；PDF 可下載
- [ ] （migration 後）履歷含 **完整模型 prompt**（新圖）

### D3. 獨立工具（各測一項即可）

- [ ] `/pattern-extract/` 或 `/design-to-physical/` 或 `/scene-sim/` — 至少一項生圖成功
- [ ] `/promo-image/` **情境圖** 生圖成功 → 資產庫「情境圖」分頁
- [ ] `/promo-camera` **商攝導演** 生圖成功 → 同上

### D4. 材料組合與印花

- [ ] `/client/material-dual-color.html` 生圖 → 資產庫「材料組合」
- [ ] `/client/print-asset.html` 上傳原圖（免費）→ 資產庫「印花」
- [ ] （可選）印花 AI 重繪扣點成功

### D5. 公開分享

- [ ] 設計稿勾選媒體牆後 `/inspiration/user_design/{id}` 可開
- [ ] OG／分享連結非 404

---

## E. ② 製造商 — 煙霧測試

使用 **已綁定 manufacturers** 的帳號。

### E1. 公開頁

- [ ] `/vendor-profile.html?id=` 作品集、素材庫可見
- [ ] 「用此廠商版型設計」深鏈進設計頁

### E2. 後台工作流

- [ ] `/client/manufacturer-portfolio.html` 上傳或編輯一筆展示案例
- [ ] `/client/manufacturer-materials.html` 上傳或編輯一筆素材（付費帳號）
- [ ] 素材 **AI 重繪** 或 **放大預覽** 至少一項可用

### E3. B 線（若該帳號已有展示案例）

- [ ] `/client/industry-suppliers.html` 可瀏覽
- [ ] 匯入一筆 → `/client/my-supplier-references.html` 可見

### E4. Embed（若為付費廠商）

- [ ] 素材庫取得 iframe 嵌入碼
- [ ] `/embed/simulator.html?…` 訪客試做生圖成功（廠商扣點）
- [ ] `/client/embed-design-records.html` 有紀錄；**履歷**可開

---

## F. ③ 產業供應商 — 煙霧測試

- [ ] `/client/supplier-catalog-manage.html` 可上架一筆（付費供應商帳號）
- [ ] `/client/industry-supplier-dashboard.html` 引用紀錄可載入

---

## G. 管理員後台

使用 **admin** 帳號。

### G1. 營運

- [ ] `/admin/platform-stats.html` 各 tab 載入；表尾 **總計** 列正常
- [ ] 情境圖統計可區分 **情境圖頁 / 商攝導演**
- [ ] `/admin/generation-records.html` 列表、詳情 modal 正常
- [ ] 勾選 2 筆 → **匯出選取 ZIP** 可下載且可解開

### G2. 內容字典（抽測）

- [ ] `/admin/custom-categories.html` 可開
- [ ] `/admin/promo-scene-templates.html` 可開
- [ ] `/admin/material-color-palettes.html` 可開（若已跑 migration）
- [ ] `/client/manufacturer-materials.html?official_platform=1&manage=1` 官方版型庫可開

### G3. 會員／點數

- [ ] `/admin/membership.html` 可查看／手動調點（測試用）
- [ ] `/admin/user-management.html` 可搜尋測試帳號

---

## H. 金流與權限

- [ ] **免費帳號**上傳素材／供應商品項 → 預期 403 或頁內提示（限制 A）
- [ ] **無展示案例的製造商**匯入 B 線 → 預期阻擋（限制 B）
- [ ] PayPal 儲值或訂閱（正式或 sandbox）走完一輪 → 點數到帳
- [ ] 生圖失敗時 **未異常扣點**（抽測一項即可）

---

## I. SEO 與公開 URL（抽測）

- [ ] `/official-templates/` 真列表頁可開（非 301 進設計頁 tab）
- [ ] `/vendor-styles/` 真列表頁可開
- [ ] `/sitemap.xml` 可開且子 sitemap 非空
- [ ] `/client/manufacturer-dashboard.html` 等個人後台 **noindex**（檢視原始碼 `robots`）
- [ ] `/promo-camera?embed=design` **noindex**；獨立 `/promo-camera` 可索引

---

## J. 多語系（可選，不擋主線）

- [ ] 首頁或工具頁 `?lang=en` 主要導覽為英文
- [ ] 官方配色／情境模板英文名稱有顯示（若已維護 `name_en`）

---

## K. 已知不擋上線（上線後 backlog）

以下**不需**在本表全部勾完才可上線：

- 生圖履歷舊資料 backfill
- 設計風向（測試中）
- 廠商內容英文自動翻譯 UI
- Embed CAPTCHA／域名白名單
- 攝影 App Store／IAP
- SEO Phase D 內容債

---

## L. 簽核

| 項目 | 值 |
|------|-----|
| 上線日期 | |
| 部署 commit | |
| 執行者 | |
| Migration 已跑 | |
| 阻擋問題（若有） | |
| 結論 | ☐ 可上線　☐ 延後（原因：________） |

---

*本表隨功能迭代更新；與 `docs/deploy-matchdo-push-and-deploy.md` 衝突時，部署指令以 deploy 文件為準。*
